import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frbastin.breviary',
  appName: 'Catholic Breviary',
  webDir: 'dist'
};

export default config;
